name: Build Mommy Turbo
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🛠️ 暴力推土机：全项目包名无死角覆盖
        run: |
          # 1. API 注入
          find TMessagesProj/ -name "BuildVars.java" -exec sed -i 's/public static int APP_ID = .*/public static int APP_ID = 37274250;/g' {} +
          find TMessagesProj/ -name "BuildVars.java" -exec sed -i 's/public static String APP_HASH = .*/public static String APP_HASH = "5205cbdae6cecc6ce1bd494b5685fe16";/g' {} +

          # 2. ⚡ 32 线程提速 (Turbo 灵魂)
          find TMessagesProj/ -name "FileLoader.java" -exec sed -i 's/currentUploadOperationsCount < [0-9]/currentUploadOperationsCount < 32/g' {} +
          find TMessagesProj/ -name "FileLoadOperation.java" -exec sed -i 's/currentDownloadOperationsCount < [0-9]/currentDownloadOperationsCount < 32/g' {} +
          
          # 3. 🚀 【暴力核心】全文件地毯式搜索并替换
          # 不管是 java, xml, gradle, 只要包含旧包名，全部改掉
          # 这样做不会因为“重复定义”报错，因为我们是原地替换
          OLD_BETA="tw.nekomimi.nekogram.beta"
          OLD_STABLE="tw.nekomimi.nekogram"
          OLD_ORG="org.nekogram.messenger"
          NEW_APPID="org.mommy.messenger"

          find . -type f -exec sed -i "s/$OLD_BETA/$NEW_APPID/g" {} +
          find . -type f -exec sed -i "s/$OLD_STABLE/$NEW_APPID/g" {} +
          find . -type f -exec sed -i "s/$OLD_ORG/$NEW_APPID/g" {} +

          # 4. 【清理后缀】防止 Gradle 动态拼接 .beta
          find . -name "*.gradle" -type f -exec sed -i 's/applicationIdSuffix ".beta"//g' {} +
          find . -name "*.gradle" -type f -exec sed -i 's/applicationIdSuffix ""//g' {} +
          
          # 5. 【改名】Neko -> Mommy
          find . -name "strings.xml" -type f -exec sed -i 's/Nekogram/Mommy/g' {} +
          find . -name "strings.xml" -type f -exec sed -i 's/Neko/Mommy/g' {} +

          # 6. 注入 Google Services JSON
          echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > TMessagesProj_App/google-services.json
          
          # 7. 内存调优
          echo "org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8" >> gradle.properties

      - name: set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: 🏗️ 强力编译模式
        run: |
          chmod +x gradlew
          # 这里不再手动传 -PapplicationId，完全依赖上面 sed 替换后的结果
          ./gradlew clean assembleDebug --no-daemon

      - name: 📦 导出 Mommy 安装包
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Mommy-Turbo-APK
          path: TMessagesProj_App/build/outputs/apk/debug/*.apk
