name: Build Mommy Turbo
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🛠️ 终极手术：参数强制注入模式
        run: |
          # 1. 注入你的 API
          find TMessagesProj/ -name "BuildVars.java" -exec sed -i 's/public static int APP_ID = .*/public static int APP_ID = 37274250;/g' {} +
          find TMessagesProj/ -name "BuildVars.java" -exec sed -i 's/public static String APP_HASH = .*/public static String APP_HASH = "5205cbdae6cecc6ce1bd494b5685fe16";/g' {} +
          
          # 2. 全局改名：将所有 Nekogram 文本替换为 Mommy
          grep -rl "Nekogram" . | xargs sed -i 's/Nekogram/Mommy/g' || true
          
          # 3. 内存与性能优化
          echo "org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8" >> gradle.properties
          echo "android.extractNativeLibs=true" >> gradle.properties

          # 4. 【关键】全量强制替换包名字符串
          # 这一步把代码里所有可能的旧包名全部换成新的，不留死角
          find . -type f \( -name "*.gradle" -o -name "*.xml" -o -name "*.java" \) -exec sed -i 's/tw.nekomimi.nekogram/org.mommy.messenger/g' {} +
          find . -type f \( -name "*.gradle" -o -name "*.xml" -o -name "*.java" \) -exec sed -i 's/org.nekogram.messenger/org.mommy.messenger/g' {} +

          # 5. 【补丁】删除可能导致冲突的 Sentry (刚才报错里提到的 Warning 源)
          # 既然我们不需要监控，直接把这一段干掉，省得出错
          find . -name "AndroidManifest.xml" -exec sed -i '/SentryInitProvider/d' {} +
          find . -name "AndroidManifest.xml" -exec sed -i '/SentryPerformanceProvider/d' {} +

          # 6. 注入私密 JSON
          echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > TMessagesProj_App/google-services.json

      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🏗️ 强制参数编译
        run: |
          chmod +x gradlew
          # 使用 -P 强制覆盖所有模块的 applicationId
          ./gradlew assembleDebug --no-daemon \
            -PapplicationId=org.mommy.messenger \
            -PMS_APP_ID=org.mommy.messenger

      - name: 📦 导出 Mommy 安装包
        uses: actions/upload-artifact@v4
        with:
          name: Mommy-Turbo-APK
          path: TMessagesProj_App/build/outputs/apk/debug/*.apk
